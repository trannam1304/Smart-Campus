# Smart Campus Facility & Study Room Reservation Platform

Khung dự án khởi tạo — chưa có code, chỉ có cấu trúc thư mục theo kiến trúc đã thống nhất.

## Cấu trúc thư mục

- `backend/` — API service (Node.js/NestJS hoặc tương đương), chia module theo 6 nhóm chức năng (FR-1.x → FR-6.x)
- `frontend/` — React app, giao diện sinh viên & admin/staff
- `docs/` — Tài liệu đặc tả (SRS ISO 29148, ERD, API spec)
- `.github/workflows/` — CI/CD pipeline

## Bắt đầu

```bash
# Backend
cd backend && npm install

# Frontend
cd frontend && npm install
```

## Nhánh Git
Xem quy ước nhánh tại `docs/BRANCHING.md`.
